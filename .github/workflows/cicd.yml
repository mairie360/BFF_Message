name: BFF Message CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@v1.13.2
    with:
      package_name: bff-message
      node_version: "22"
      cicd_version: v1.13.2
    secrets: inherit